import {useState} from "react";import Icon from "../components/icon";import diary from "../data/diary";
function Diary(){const[selected,setSelected]=useState(null);return <section className="page-shell"><div className="page-intro"><div><p className="eyebrow">03 / DIARY</p><h1>Notes from the<br/><span>things I’m building.</span></h1></div><p className="page-intro-copy">A personal corner for build logs, design notes, lessons, experiments, and thoughts that deserve more than a disappearing chat message.</p></div><div className="diary-grid">{diary.map((entry,i)=><article className="diary-card" key={entry.id} onClick={()=>setSelected(entry)}><div className="diary-card-top"><span>{String(i+1).padStart(2,"0")}</span><span>{entry.category}</span></div><div><h2>{entry.title}</h2><p>{entry.excerpt}</p></div><div className="diary-card-bottom"><div className="tag-list">{entry.tags.map(tag=><span className="tag" key={tag}>{tag}</span>)}</div><Icon name="arrow" size={18}/></div></article>)}</div>{selected&&<div className="modal-backdrop" onClick={()=>setSelected(null)}><article className="diary-modal" onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setSelected(null)} aria-label="Close"><Icon name="close"/></button><p className="eyebrow">{selected.category}</p><h2>{selected.title}</h2><p className="modal-body">{selected.body}</p><div className="tag-list">{selected.tags.map(tag=><span className="tag" key={tag}>{tag}</span>)}</div></article></div>}</section>}
export default Diary;